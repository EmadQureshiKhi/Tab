import { WORKSPACE_ID } from "@tabai/shared";

/** Dashboard workspace placeholder. Routes and components arrive with task 19. */
export const WORKSPACE_ID_APP = "@tabai/app" as const;

export const SHARED_WORKSPACE_ID = WORKSPACE_ID;
