{
  "name": "tab",
  "version": "0.0.0",
  "private": true,
  "description": "Post-paid billing and a credit facility for autonomous agents, built natively for Creditcoin.",
  "license": "MIT",
  "packageManager": "pnpm@9.15.3",
  "engines": {
    "node": ">=20.10.0",
    "pnpm": ">=9.15.3"
  },
  "scripts": {
    "build": "turbo run build",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "lint": "node scripts/check-dep-direction.mjs && node scripts/env-check.mjs && turbo run lint",
    "lint:deps": "node scripts/check-dep-direction.mjs",
    "coverage": "node scripts/check-coverage.mjs --run",
    "coverage:check": "node scripts/check-coverage.mjs",
    "coverage:floors": "node scripts/check-coverage.mjs --list",
    "env:bootstrap": "node scripts/env-bootstrap.mjs",
    "env:check": "node scripts/env-check.mjs",
    "env:list": "node scripts/env-check.mjs --list",
    "deployments:check": "node scripts/deployments-check.mjs",
    "deployments:classify": "node scripts/deployments-check.mjs --list",
    "vocab:check": "node scripts/vocab-check.mjs",
    "vocab:denylist": "node scripts/vocab-check.mjs --materialise",
    "tab:verify": "node scripts/tab-verify.mjs",
    "hooks:install": "git config core.hooksPath .githooks",
    "art": "node scripts/art.mjs",
    "art:check": "node scripts/art.mjs --check",
    "e2e": "bash scripts/e2e.sh"
  },
  "dependencies": {
    "@gluwa/usc-sdk": "0.18.0",
    "ethers": "^6"
  },
  "devDependencies": {
    "@gluwa/usc-contracts": "0.1.2",
    "@openzeppelin/contracts": "5.4.0",
    "@tabai/sdk": "workspace:*",
    "tsx": "4.19.2",
    "turbo": "2.10.12",
    "typescript": "5.8.3"
  },
  "pnpm": {
    "overrides": {
      "@gluwa/usc-sdk": "0.18.0",
      "@gluwa/usc-contracts": "0.1.2",
      "@openzeppelin/contracts": "5.4.0"
    }
  }
}
