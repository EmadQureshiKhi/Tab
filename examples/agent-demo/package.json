{
  "name": "@tabai/agent-demo",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Two agents trading over the rail end to end, one metered call and one settlement each.",
  "license": "MIT",
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc -b",
    "test": "tsc -b && node --test test/*.test.mjs",
    "demo": "tsx src/main.ts"
  },
  "dependencies": {
    "@tabai/sdk": "workspace:*",
    "@tabai/shared": "workspace:^",
    "ethers": "6.17.0"
  },
  "devDependencies": {
    "@types/node": "22.20.1",
    "tsx": "4.19.2"
  }
}
